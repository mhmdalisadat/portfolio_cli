version: "3.8"

services:
  portfolio-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1297:1297"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: portfolio-cli-app
