version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3366:3366"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: portfolio-cli-frontend
